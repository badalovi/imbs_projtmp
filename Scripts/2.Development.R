
#------------------------------------------------

imbs_tune_method(data       = arg_data,
                 method     = arg_method,
                 model      = arg_model,
                 model_tune = arg_model_tune,
                 formula    = arg_formula,
                 val_method = arg_val_method,
                 criteria   = arg_criteria
                 )

#------------------------------------------------

imbs_tune_model(data      = arg_data,
                model     = arg_model,
                formula   = arg_formula,
                model_par = arg_model_par,
                metrics   = arg_metrics,
                val_model = arg_val_model,
                tune_ln   = arg_tune_ln
                )

#------------------------------------------------